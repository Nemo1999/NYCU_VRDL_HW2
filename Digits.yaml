# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: ../datasets/Digits  # dataset root dir
train: train/images  
val: validate/images  
test:  test/images 

# Classes
nc: 11  # number of classes
names: ['Invalide label', 'one', 'two', 'three', 'four', 'five', 'six' , 'seven', 'eight', 'nine', 'zero']  # class names